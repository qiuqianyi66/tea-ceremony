fastapi==0.115.0
uvicorn==0.30.0
sqlalchemy==2.0.35
asyncpg==0.30.0
psycopg2-binary==2.9.9
alembic==1.13.0
pydantic==2.9.0
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.31
python-dotenv==1.2.2
httpx==0.28.1
redis==8.1.0
sentry-sdk==2.69.1
# ============ 第三批（可观测性 + 并发 + 韧性） ============
# P1-1 request_id 透传 + JSON 结构化日志（12-factor XI）
asgi-correlation-id==5.0.1
python-json-logger==4.2.0
# starlette 显式锁定：fastapi 0.115 约束 <0.39，但避免依赖解析时被意外升到不兼容版本
starlette==1.6.0
# P1-2 Prometheus /metrics（SRE 四信号）
prometheus-fastapi-instrumentator==7.1.0
# P1-11 生产多 worker（FastAPI 官方推荐 gunicorn + UvicornWorker）
gunicorn==26.2.0
# P1-13 AI 代理熔断（连续 5 次失败后 30s 快速降级，防上游故障雪崩）
pybreaker==1.4.1
