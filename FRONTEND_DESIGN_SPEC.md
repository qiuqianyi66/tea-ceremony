# 前端设计规范（Frontend Design Spec）— 通用版

> 适用：任何前端项目（React / Vue / 原生 / 小程序），任何 AI 编码工具（Claude Code / Codex / Cursor / Gemini CLI / Copilot）。
> 来源：Anthropic frontend-design、taste-skill、impeccable、ui-ux-pro-max 四家之长融合。
> 用法：复制本文件到项目根目录，在设计任务开始前通读。规范是活文档，踩坑往最后加。

## 0. 角色与验收

* 角色：设计总监——给每个产品一个"不会被误认成别人的"视觉身份。客户已经拒绝过陈词滥调。
* 验收标准：**一眼认不出是模板，也认不出是 AI 生成的**。两条都过才算完成。
* 简报优先：用户明说方向时，简报的原话永远赢；禁止把你的品味压过简报。

## 1. 设计流程（先方向，后代码）

任何页面 / 组件设计，走两遍：先出设计计划并对照简报评审，评审通过才写码。

### 1.1 Design Read（开工前先读场，一行声明）

写码前必须输出一行：

> Reading this as: `<页面类型>` for `<受众>`，with a `<vibe>` language，leaning toward `<设计系统或审美家族>`。

* 页面类型：landing / portfolio / app UI / dashboard / editorial / redesign。
* 受众决定审美，不是你的品味：B2B 采购组 vs 设计敏感消费者 vs 招聘官扫简历。
* 安静约束优先于审美：无障碍优先、公共部门、受监管行业、儿童产品、信任优先电商——这些场景的约束压过一切风格偏好。
* 简报含糊且方向真的分叉时，只问**一个问题**；能推理就不问。

### 1.2 三拨盘（核心配置）

Design Read 之后，为全页设三个拨盘，所有布局 / 动效 / 密度决策受它们约束：

| 拨盘 | 范围 | 含义 |
|---|---|---|
| `DESIGN_VARIANCE` | 1-10 | 1=完美对称，10=艺术混沌 |
| `MOTION_INTENSITY` | 1-10 | 1=完全静态，10=电影级物理 |
| `VISUAL_DENSITY` | 1-10 | 1=画廊留白，10=驾驶舱塞满 |

* 基线 **8 / 6 / 4**，除非 Design Read 推翻。
* 信号→拨盘：极简 / 编辑风 → 5-6 / 3-4 / 2-3；高级消费 / Apple 系 → 7-8 / 5-7 / 3-4；玩票 / Awwwards → 9-10 / 8-10 / 3-4；信任优先 / 公共部门 → 3-4 / 2-3 / 4-5；redesign 保留 → 对齐现状、动效 +1；redesign 推翻 → 两项 +2。
* 拨盘是全局变量，全文引用，禁止另起别名。

### 1.3 视觉方向与令牌系统

* 用一两句话写清本页视觉方向（"清晨茶园的水墨留白""炭火与陶土的粗粝感"）。
* 写"这页绝不出现的默认值"清单（防 distributional convergence，即模型向通用模板收敛）。
* 令牌系统：4-6 个命名色值 + 字体角色（标题体 / 正文体）+ 布局概念（一句话 + ASCII 线框）+ 设计原则。
* 对照简报评审计划：哪部分像"任何项目都能用的默认"就改，说清改了什么、为什么。
* 勇气只花在一个地方：一个元素负责让人记住，周围全安静。Chanel 规则：出门前照镜子摘掉一件配饰。

## 2. 体系选择（诚实区分系统与审美）

* **真实设计系统用官方包**：Fluent（MS）/ Material 3 / Carbon（IBM）/ Polaris（Shopify）/ Atlassian / Primer（GitHub）/ GOV.UK / USWDS / Bootstrap / Radix / shadcn（可深度定制，禁止默认态）。一个项目只用一个系统，禁止混装（Fluent + Carbon 同树）。
* **审美方向没有官方包**，用原生 CSS + Tailwind 实现，注释里诚实标注"借鉴灵感 vs 官方材料"：玻璃拟态（backdrop-filter + 实色降级）、Bento、Brutalist、编辑风、暗黑科技、Aurora 渐变、动力学排版、Apple Liquid Glass（**官方无 web 版，一律标注近似**）。

## 3. 禁令（AI 默认脸，全量禁止）

### 配色
* AI 紫 / 蓝紫渐变禁默认（#6366F1、#8B5CF6）；品牌明确要紫除外，但要有意图地做。
* 暖米白底 + 陶土/赭石强调禁默认（#F5F1EA + #D97757 系，模型最大特征之一）。
* 纯黑 / 纯灰禁——一律 tint（带色相的深色，#0B0B0B 冒充黑也是特征）。
* Tailwind 默认色板禁；一页只许一个强调色，全页锁定（色彩一致性锁），禁止中途换色。

### 字体
* Inter / 系统默认字体禁默认；优先 Geist、Outfit、Satoshi、Cabinet Grotesk 或品牌衬线。
* **衬线体默认禁**——"创意简报 = 衬线"是测试中最常见的 AI 特征；只有品牌点名或真编辑/奢侈/遗产语境才用，且要能说清为什么是这一款。
* Fraunces、Instrument_Serif 两个 AI 最爱衬线**直接禁**。
* 系统展示字体（Impact、Arial Black、平台 sans）禁当展示字。
* 标题内强调用同字体的斜体 / 粗体，禁止混插异族字体。
* 斜体 descender 不裁切：leading ≥ 1.1，y/g/j/p/q 结尾的斜体词加底部余量。

### 布局
* Hero + 三卡片禁；同尺寸"图标+标题+正文"卡片网格禁（卡片是懒惰容器，卡片套卡片永远错）。
* 眉题 eyebrow（标题上方小标签）**绝对禁**——标题自己撑得住，删掉。
* 01 / 02 / 03 编号除非内容真是序列（分步流程 / 时间线）。
* hero 指标模板（大数字 + 小标签 + 佐证 + 渐变强调）禁默认。
* 等宽多栏、完美居中禁；允许打破网格（文字压图、超大字重、错位留白）。

### 组件与装饰
* 玻璃拟态当装饰禁（只作特定效果用）。
* 渐变文字禁——强调来自字重或字号。
* 彩色侧边条（border-left/right > 1px）禁。
* 硬偏移阴影（box-shadow: 4px 4px 0）非真新粗野世界禁。
* Sparklines / 进度环 / 圆角占位框当内容禁。
* 等宽字体当"技术感戏服"禁——只用于代码 / 数据 / 度量。
* Unicode 符号 / emoji 当图标禁——用图标库或自绘 SVG，一个家族统一描边粗细。
* 几何蒙版（圆形 / 多边形 / 径向渐变抠图）替代真实主体轮廓禁——要抠图就用 alpha 蒙版。
* 明暗模式按使用场景选（谁、在哪、什么环境光），禁止按品类习惯选。

### 动效
* 每个区块同款淡入上滑入场禁；**一个编排时刻**（一次页面加载序列或一个 reveal）胜过满屏散弹。
* bounce / elastic 缓动禁（显旧）。
* 悬停动图禁——图片不是动作目标，反馈给容器。
* 动效必须回答用户动作（打开 / 展开 / 确认），只画响应不画装饰。

### 文案
* Lorem Ipsum 禁；高深名词和空话禁（"提升用户体验"这类）。
* 被动语态禁；错误不道歉、不模糊——说清发生了什么、怎么恢复。
* 空态是行动邀请，不是情绪。
* 界面词汇全流程一致：按钮"发布"，toast 就叫"已发布"。

## 4. 质量底线（craft floor，逐项可验证）

* 对比度：正文 / 占位 ≥ 4.5:1，大字 ≥ 3:1；彩色底上的次要文字从该色相派生，**禁用灰色**。
* 阴影：带偏移 + 软模糊；零偏移彩色光晕是装饰不是深度。
* 间距：紧凑分组、组间大方；标题上方空间 > 标题下方空间。
* 字体度量：正文 65-75ch；display 最大 6rem；tracking ≥ -0.04em；每个断点跑真实文案查溢出。
* 状态齐全：hover / disabled / loading / error / empty 五态 + 真实内容 + 可用的控件 + 键盘焦点。
* **浏览器表面定制**：文本选区、光标、滚动条、焦点环、下划线偏移、表格数字——这些"你没画的部分"全部上主题色。这是"人做的 vs 拼装的"最便宜的信号，也是模型最常漏掉的一项。
* 动效尊重 prefers-reduced-motion；提供降级（透明降级给 prefers-reduced-transparency）。

## 5. 工程规范（栈无关）

* 引入任何第三方库前先查 `package.json`；缺了就输出安装命令，禁止假设库存在。
* 全屏 Hero 禁 `h-screen`，用 `min-h-[100dvh]`（iOS Safari 地址栏跳动）。
* 复杂布局用 CSS Grid，禁止 flex 百分比数学（`w-[calc(33%-1rem)]`）。
* 图标一个库一个家族，全局统一 strokeWidth；禁止手绘 SVG 路径。
* 字体自托管（@font-face + font-display: swap），生产环境禁止 `<link>` 引 Google Fonts。
* 触摸目标 ≥ 44×44px；断点标准（sm 640 / md 768 / lg 1024 / xl 1280）。
* 禁止用 useState 跟踪鼠标位置 / 滚动进度这类连续值（重渲染崩溃移动端）——用 motion value / transform。
* 连续值驱动的动画用 rAF / 库内机制，禁止 setInterval 高频刷新。

## 6. 设计审计（提交前截图逐条过）

1. 去掉 logo 还认得出品牌吗？认不出 = 没性格。
2. 会被当成模板 / AI 生成吗？会就改掉最像模板的那个元素。
3. 三拨盘落实了吗？有没有越出 Design Read 定的范围？
4. 浏览器表面定制了吗（选区 / 滚动条 / 焦点环）？
5. 色彩一致性锁了吗？有没有第二个强调色漏进来？
6. 数字、场景、文案具体吗？动效有意图还是纯装饰？
7. 手机端可读、可点、无重叠裁切？
8. 反模式清单（§3）逐条过一遍，命中的都改。
9. 质量底线（§4）逐项验证，不止"看起来对"。

## 7. 设计工作流命令（对应 impeccable，按需执行）

* 新页面 / 重构：先 `shape`（规划 UX/UI）→ 再写码。
* 存产品上下文：`init`（PRODUCT.md：受众 / 品牌赛道 / 声音 / 反参考 / 颜色 / 字体 / 组件）。
* 评审：`critique`（UX 设计评审：层级 / 清晰 / 情感共鸣）→ `audit`（a11y / 性能 / 响应式）。
* 收尾：`polish`（对齐设计系统、可交付）。
* 调性：`bolder`（太安全就放大）→ `quieter`（太吵就收）→ `distill`（删到本质）。
* 专项：`harden`（错误 / i18n / 溢出边界）→ `onboard`（首登 / 空态）→ `animate` → `colorize` → `typeset` → `layout` → `delight` → `clarify`（UX 文案）。
* 迭代：`live`（浏览器里出变体，选中元素生成替代方案）。

## 8. 学习记录（AI 维护）

用户纠正过的做法，往这里加一行"总是用 X 做 Y"。已有规则覆盖就先收紧，不重复加。

* （空）

---

*活文档：每几周修剪一次，只留"删掉会犯错"的规则。*
