# tea-testing 副本存档

> 来源：桌面旧工作副本 `C:\Users\yanha\Desktop\tea-testing\`，于 2026-09-10 删除后归档于此。

## 为什么留这些

`tea-testing` 是项目早期在桌面另开的一份克隆（同一远程 `qiuqianyi66/tea-ceremony`），停更于 2026-09-05 的 `feature/immersive-home` 分支（`0d883e2`）。该分支经 PR #11 squash 合并进 main（对应 `90b3602`），代码已全部在 git 历史和 GitHub 远程中，本地副本本身没有独立价值。

这里只保留了原仓库中**未被 git 跟踪、且 main 上不存在**的过程稿：

- `plans/` —— 2026-09-05 前后做 3D 视觉 / 泡茶动画 / 冲泡流程修正+茶产区地图的需求计划文档，记录了当时的问题诊断和决策理由。
- `pr-body*.md` —— 对应几个 PR 的描述草稿（正式 PR 描述已在 GitHub）。
- `screenshots/` —— UI 方向探索稿（玻璃 / 侘寂 / 胶片三种方向）、用户反馈截图、Pexels 茶壶图和两张用户参考图。

## 注意

- 这些是**历史过程稿**，不是当前实现的事实来源。当前行为以 `src/`、`backend/`、`CONTEXT.md`、`DESIGN_SPEC.md` 为准。
- 不要把这里的截图或计划当作最新状态引用。
